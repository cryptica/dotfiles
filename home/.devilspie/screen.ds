(if
    (and
        (is (application_name) "terminator")
        (is (window_name) "SCREEN")
    )
    (begin
        (skip_tasklist)
        (skip_pager)
        (undecorate)
        (below)
        (stick)
        (geometry "1010x710+1920+500")
    )
)

